# This file is a placeholder for environment variables required to run this project.
# Currently, there are no environment variables needed.

# Example of what future variables might look like:
# NEXT_PUBLIC_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
# SECRET_KEY=your_secret_key_here

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password

# Portfolio chatbot (optional). Leave both unset to hide the chat button.
# Public URL of the portfolio-chat-proxy (Tailscale funnel on :8443 typically).
NEXT_PUBLIC_CHAT_URL=
# Obfuscation token matching the wrapper's SHARED_TOKEN. NOTE: ships to the
# browser — this is a speed bump for casual scrapers, not real auth.
NEXT_PUBLIC_CHAT_TOKEN=
